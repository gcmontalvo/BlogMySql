{% extends "Layout.html.twig"%}

{% block content %}
    <div class="container-fluid">
        <h2>Etiqueta</h2>
        <div class="col-lg-6">
            <table class="table table-striped table-sm table-bordered table-hover">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ tag.id }}</td>
                    </tr>
                    <tr>
                        <th>Nombre</th>
                        <td>{{ tag.name }}</td>
                    </tr>
                    <tr>
                        <th>Descripción</th>
                        <td>{{ tag.description }}</td>
                    </tr>
                </tbody>
            </table>
            
            {{ form_start(delete_form) }}
            <div class="form-group">
                <a class="btn btn-secondary" href="{{ path('tag_index') }}">Volver</a>
                <a class="btn btn-primary" href="{{ path('tag_edit', { 'id': tag.id }) }}">Editar</a>
                <input type="submit" class="btn btn-danger" value="Eliminar">
            </div>
            {{ form_end(delete_form) }}
            
        </div>
    </div>
{% endblock %}
